package Arreglos;

public class Consejos {
    public static void main(String[] args) {
        // cuando tengo que borrar en el bucle, cuando se borra la secuencia el fin se iguala al inicio
        /*
        while (inicio < MAX) {
            inicio = buscarInicio(arr, fin+1);
            if (inicio < MAX) {
                fin = buscarFin(arr, inicio);
                tamañoSec = tamañoSecuencia(arr, inicio, fin);
                if (tamañoSec == numeroN) {
                    eliminarSecuencia(arr, inicio, tamañoSec);
                    fin = inicio;
                }
            }
        }
        */
    }
}
